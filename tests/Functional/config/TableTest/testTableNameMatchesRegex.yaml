scanners:
    -
        label: startsWithDragon
        scanner: table
        assertions:
            - { assertion: tableNameMatchesRegex, pattern: /^dragon_/i }
    -
        label: noUnderscore
        scanner: table
        assertions:
            - { assertion: tableNameMatchesRegex, pattern: /^[^_]+$/ }
